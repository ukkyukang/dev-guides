[project]
name = "my-app"
version = "0.1.0"
description = "컨테이너 개발 예제 — Streamlit 앱"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.40.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/my_app"]
