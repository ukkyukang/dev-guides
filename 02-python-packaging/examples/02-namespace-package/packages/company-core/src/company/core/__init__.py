"""company.core — 핵심 데이터 모델 패키지.

이 패키지는 조직 전체에서 공유하는 핵심 데이터 모델을 제공합니다.

Example:
    >>> from company.core.models import Product
    >>> product = Product(name="노트북", price=1500000)
    >>> print(product)
    Product(name='노트북', price=1500000)
"""

__version__ = "1.0.0"
